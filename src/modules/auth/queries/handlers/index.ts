import { ValidateUserHandler } from './validate-user.handler';
import { ValidateUserRolesHandler } from './validate-user-roles.handler';

export const QueryHandlers = [
  ValidateUserHandler,
  ValidateUserRolesHandler,
];
